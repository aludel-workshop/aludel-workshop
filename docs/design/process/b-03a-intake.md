# B-03A supervised portal cycle — 2026-09-20

Owner requests that future ideas, authorizations and answers enter the running app; Codex temporarily performs worker duties after an owner notification. This authorizes the local bridge implementation, not future work items or external effects.

Process gap: Markdown next_action and conversational “go” can diverge from product authorization. Replace dispatch-by-document with persisted portal state, exact version checks, and actor-specific operations shared by browser and local operator. Keep repository status as engineering handoff only.

Readiness: ready for this bounded local implementation using accepted MD3 v3 shell, Work ownership, proposal editor, status and disclosure patterns. Reuse existing Material controls and product-detail styles. Work list discloses scope, acceptance, boundary and event history; questions and owner answers stay with the affected item. No separate navigation area. Empty list explains intake; multiple tasks remain individually actionable; conflicts retain typed answers and require refresh. The existing request entry remains primary intake. Review completion here means evidence submitted, not artifact acceptance or release.

Sequence: transactional domain + operator CLI → authenticated owner API → Work and request inbox UI → domain and browser checks → running-app refresh → operating guide and status closeout. Required checks: unauthorized/unclaimed work rejected, duplicate/stale operations rejected, question answer does not expand scope or automatically resume, cancellation stops reporting, persistence, owner/worker operation separation, wide/narrow UI.

Deferred to B-03B: real automated adapter, leases/heartbeat/recovery, tracker/provider reconciliation, immutable preview hosting and full candidate review. This bridge must expose its manual worker honestly and must not claim those capabilities passed. Interface owner acceptance follows the first real portal cycle.
