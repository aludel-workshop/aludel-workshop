# D-01B v4 work record — 2026-09-19

Outcome: test the accepted project workspace with a local, simulated BorrowBox interaction. Authorized effects: new versioned local prototype and evidence only. Owner feedback is required for packet completion.

Process assessment: the existing operating procedure is adequate. Prior versions confused collection scope, page ownership and prototype controls. Apply the accepted contracts as a transition-to-check ledger before construction; preserve v3 and use a separate storage key. This record tests the procedure's application, not its general effectiveness.

Inputs read: D-01D structure revision 1 and scoped review (DEC-016 accepted for prototype); prototype-readiness v1 (ready); D-01C v2 opening hierarchy (DEC-012/013 accepted); BorrowBox fixture (fictional); D-01A lifecycle (checked design input). D-01D overrides the older Releases navigation and the old independent BB-003 fixture. BB-003 now means dependent reservation checks.

Included: Overview, Work/Decisions/Reviews collections, BB-D01, BB-001 and immutable candidates. C1–C10 own the corresponding regions. Request creation C11 and additional product areas are outside this readiness experiment. Supporting BB-002/003 rows provide context, not additional executable tasks. Experimental variables: interaction feel, disclosure, authorization checkpoint, evidence threshold. Q-002 is unresolved; fixture authorization cannot answer it.

Dependency order: verify accepted contracts → construct isolated v4 → exercise model and browser transitions → capture identity/evidence → owner review. No structural gap found at intake. Verdict: ready within the existing readiness document. Return to D-01D if a new responsibility is needed.

Check ledger: T1/T2 decision stays put, zero/one/many affected collections, independent BB-002; T3 explicit authorization then queue confirmation; T4 answer then separate continuation; T5 uncertain cancellation blocks retry; T6 linked retry; T7 immutable candidate route; T8 current/evidence guards; T9 feedback retained with new task revision; T10 changed inputs preserve old candidate; T11 filter return/deep links. Browser checks also cover refresh, narrow layout, labels and keyboard focus. Results and limitations will be recorded in the evidence report.

## Applied result

V4 was constructed without reopening accepted structure or changing v3. Five model tests and the browser transition ledger passed; exact results, screenshots and limitations are in [D-01B evidence](../../evidence/d-01b-prototype-review.md). Unsaved form drafts are retained through same-session navigation/reload; writes to saved fixture state complete before the visible state advances. Desktop/narrow review screenshots were inspected.

Process outcome: the existing gate yielded an inspectable experiment with distinct page responsibilities and checked transitions. This supports this application only, not general effectiveness or usability. Task outcome: ready for owner interaction review, not packet completion. Next action D-01B: obtain scoped feedback on the three review tasks. Stop before B-01 or any live integration; return to D-01D if feedback reveals a missing responsibility.
