Create one realistic production-quality Material Design 3 desktop web application concept, 1440 x 1024. Revise the first attached image (outcome briefing / previous option 2) using the labelled left navigation from the second attached image (previous option 1). Preserve the first image's calm readable design, outcome-first Overview hierarchy, tonal indigo accents and structured work rows. Do not reproduce its top navigation or single key-decision hero. Use the second image for LEFT NAVIGATION ONLY, never its list/detail content panes. A single coherent Overview, not inbox/detail, not option 3's oversized single-action panel. No browser chrome or external simulation harness. Date anchor 2026-09-19, avoid dates in the UI.

Aludel is the app; BorrowBox is the current managed project, a neighborhood tool library. Left navigation: Aludel wordmark at top, Projects return link, Overview (selected), Work, Decisions, Reviews. Do NOT invent Settings, Help or other destinations. In the main header show BorrowBox context and ONE quiet outlined New request action. Below, readable strong heading 'Members can reserve a tool for pickup.' and small subtitle 'A neighborhood tool library'.

Main overview section: 'Decisions needing you' with concise summary '3 open · 2 blocking · 1 optional'. It is a bounded queue, not an individual question expanded into a giant hero. One lightly tinted grouped surface or plain shared surface, with THREE compact rows separated by subtle dividers, not individual cards. Each row has a distinct readable question, concise consequence, and explicit Blocking or Optional status. Rows:
1 'Confirm reservations instantly or require approval?' — 'Affects reservation implementation and checks' — Blocking.
2 'Which pickup windows should members see?' — 'Blocks pickup instructions' — Blocking.
3 'What should we call tool categories?' — 'Optional · does not block work' — Optional.
Each title is visibly a link/openable row, with a subtle trailing arrow if useful. No inline choice form, no radio buttons, no autosave, no row select leading to a right-hand detail pane. A single filled rounded MD3 button 'Review decisions' opens the project decision queue; it can sit in the section header or footer. Do not add duplicate requests/actions. The decision stack should be immediately recognizable as several matters to work through, with no one decision monopolizing the page.

Below, a compact 'Work' summary on the base page surface. Rows show 'Reservation implementation' Blocked, 'Reservation checks' Waiting, 'Pickup instructions' Blocked, 'Improve catalog descriptions' Ready. Ready is a text status, never a completion checkmark. Work summary is secondary; no excessive detail duplication. A restrained supporting line says 'No preview yet'. No charts, progress percentages, kanban, diagnostic metadata, fake success, app preview or release control. Do not claim all work is blocked.

Use grouping, alignment, whitespace and hierarchy first, then dividers and subtle tones. No cards inside cards, no three stacked section cards, no entire app floating in a card. Material-style Roboto typography, 14–16px body baseline, clearly readable labels, consistent icon family. Quiet light surfaces and indigo primary action; no gradients, decorative illustrations or oversized empty panels. Content must fit without cropping, and should feel like the overview of a project with multiple decisions, not an inbox. Generated visual is a composition hypothesis, not code. Output one screen only.
