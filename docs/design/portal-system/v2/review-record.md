# Revised Overview — owner inspection pending

The [image](overview.png) combines the preferred outcome-led direction with left navigation and three comparable decision rows. [Manifest](manifest.json) records reference roles, exact prompt, hashes and actual dimensions; the original images remain unchanged. A convenience copy is at `tmp/the-machine-overview-concepts/overview-v2.png`.

Agent visual inspection: the page retains the project outcome, separates three open decisions into two blocking and one optional, shows consequences and individual links, provides one Review decisions action and one New request action, and keeps independent ready work visible. It does not adopt list-detail for Overview. There are four work items consistent with the scoped multi-item fixture. No run or release control appears.

Remaining visual correction: generated copy still says “Proposed outcome preview”; use “Latest preview” in the eventual implementation. The row spacing, icon choices and colors remain concept treatments subject to the real library and accessibility checks. An image cannot demonstrate expansion, navigation, focus, responsive behavior or MD3 conformance.

Owner acceptance: option 2's direction was preferred with revisions; this revised image and its interaction contract have not yet been reviewed. The [contract and retrospective](interaction-contract.md) define eight scenario walks and explicit placement choices. The original list-detail concept is not accepted for other views.

Next: inspect this refinement, then construct the bounded component/workshop slice once its renderer/runtime and scope prerequisites are checked. D-01F remains open.
