const model={phase:{id:'M1',title:'Request a change and review its preview',progress:58,source:'Product roadmap r1'},outcomes:[{id:'O-LOOP',title:'Complete the first real request → preview → review loop',progress:67,health:'At risk',source:'Product outcome r3'},{id:'O-KNOW',title:'Make product intent and work navigable in the portal',progress:46,health:'On track',source:'Product outcome r2'}],clusters:[{id:'F-WORK',outcome:'O-KNOW',title:'Work steering and planning',progress:55},{id:'F-RUN',outcome:'O-LOOP',title:'Execution and candidate review',progress:42},{id:'F-PROJECT',outcome:'O-KNOW',title:'Independent project setup',progress:12}],items:[
{id:'D-04',cluster:'F-WORK',title:'Prototype Work steering and operations',state:'Active',progress:72,phase:'Shape',priority:1,depends:[],source:'Work item · Product outcome r2 · v4 intake',action:'None',attempt:'ATT-D04'},
{id:'PW-01',cluster:'F-WORK',title:'Build project-scoped planning records',state:'Planned',progress:0,phase:'Build',priority:2,depends:['D-04'],source:'Proposed plan · consumes D-04 selection',action:'Decision required'},
{id:'B-03B',cluster:'F-RUN',title:'Integrate worker recovery and immutable review',state:'Blocked',progress:18,phase:'Build',priority:1,depends:['B-03A'],source:'Execution plan · DEC-028',action:'Owner review'},
{id:'B-03A',cluster:'F-RUN',title:'Supervised portal authorization bridge',state:'Complete',progress:100,phase:'Groundwork',priority:1,depends:[],source:'Evidence b-03a · bounded completion',action:'None'},
{id:'PW-04',cluster:'F-RUN',title:'Add contextual build inspection',state:'Planned',progress:0,phase:'Review',priority:3,depends:['PW-03','B-03B'],source:'D-02 delivery proposal',action:'Blocked'},
{id:'PW-03',cluster:'F-WORK',title:'Register design and rationale',state:'Planned',progress:0,phase:'Build',priority:4,depends:['PW-01'],source:'D-02 delivery proposal',action:'None'},
{id:'PW-05',cluster:'F-PROJECT',title:'Create project draft and setup flow',state:'Planned',progress:0,phase:'Review',priority:5,depends:['PW-01','PW-02'],source:'D-02 delivery proposal',action:'None'},
{id:'PW-02',cluster:'F-PROJECT',title:'Add product direction and roadmap authoring',state:'Planned',progress:0,phase:'Build',priority:3,depends:['PW-01'],source:'D-02 delivery proposal',action:'None'}],agents:{max:2,active:[{id:'codex-1',item:'D-04',attempt:'ATT-D04',stage:'Prototype and evidence',progress:'Browser checks next',elapsed:'18m',status:'Active'}],queue:[{item:'PW-01',rank:1,reason:'Highest priority after D-04; blocked by current design selection'},{item:'B-03B',rank:2,reason:'Milestone-critical; owner review and recovery inputs remain'},{item:'PW-02',rank:3,reason:'Depends on PW-01; product authoring precedes project setup'}],interventions:[{attempt:'ATT-B03B',item:'B-03B',status:'Awaiting input',note:'Recovery policy question blocks continuation.'},{attempt:'ATT-PW04',item:'PW-04',status:'Failed',note:'Candidate artifact identity could not be verified.'},{attempt:'ATT-B02',item:'B-02',status:'Ready for review',note:'Evidence is linked to the completed planning record.'}],history:[{attempt:'ATT-D03',item:'D-03',result:'Submitted · revision requested',duration:'31m'},{attempt:'ATT-D02',item:'D-02',result:'Submitted · structure selected',duration:'47m'}]},signals:[{id:'SIG-21',text:'Show completed and future work inside the portal',kind:'Request',age:'1d',group:'Workspace knowledge'},{id:'SIG-22',text:'Make roadmap phases, outcomes and dependencies visible',kind:'Feedback',age:'Now',group:'Work planning'},{id:'SIG-23',text:'Plan/Active/Reviews/History should not be separate features',kind:'Feedback',age:'Now',group:'Work planning'},{id:'SIG-24',text:'Need an agent queue and capacity view',kind:'Idea',age:'Now',group:'Agent operations'},{id:'SIG-25',text:'Could reviews live inside Work?',kind:'Question',age:'2h',group:'Work planning'},{id:'SIG-26',text:'Prototype should reuse actual application context',kind:'Request',age:'1d',group:'Design provenance'}]};
const main=document.querySelector('main');const fixture=()=>new URLSearchParams(location.search).get('fixture');const e=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const item=id=>model.items.find(x=>x.id===id);const cluster=id=>model.clusters.find(x=>x.id===id);const query=()=>new URLSearchParams((location.hash.split('?')[1]||''));
function setNav(){document.querySelectorAll('.sub a').forEach(a=>a.classList.toggle('selected',location.hash.startsWith(a.hash)&&a.hash!=='#/work'||location.hash==='#/work'&&a.hash==='#/work'))}
function bar(n){return `<div class="progress" aria-label="${n}% complete"><span style="width:${n}%"></span></div>`}
function state(s){return `<span class="pill ${s==='Active'?'run':s==='Blocked'?'decision':''}">${e(s)}</span>`}
function overview(){if(fixture()==='error'){main.innerHTML=`<p class="eyebrow">Work overview</p><h1>Work status is unavailable</h1><div class="error"><strong>Current state could not be loaded.</strong><p>Actions and capacity are unknown; no zero counts are shown.</p><a class="button" href="${location.pathname}#/work">Try again</a></div>`;return}main.innerHTML=`<div class="header"><div><p class="eyebrow">Work overview</p><h1>Keep the product moving.</h1><p class="lede">Your actions, current outcomes, and execution health in one place.</p></div><a class="button" href="#/plan">Open development plan</a></div><div class="banner"><div><strong>Intake review is due</strong><p>6 new signals · 3 likely part of one Work-planning theme.</p></div><a class="button" href="#/intake">Start triage</a></div><div class="grid"><section class="card attention"><h2>Needs your attention</h2><div class="list"><a class="action-row" href="#/plan?selected=PW-01"><span><strong>Select the Work implementation direction</strong><small>Unblocks PW-01 and one dependent product-authoring item</small></span><span class="pill decision">Decision</span><span>›</span></a><a class="action-row" href="#/plan?selected=B-03B"><span><strong>Review supervised-cycle evidence</strong><small>B-03B · candidate/recovery scope</small></span><span class="pill review">Review</span><span>›</span></a><a class="action-row" href="#/agents"><span><strong>Agent input requested</strong><small>Fixture example · one attempt waiting</small></span><span class="pill decision">Question</span><span>›</span></a></div></section><section class="card capacity"><h2>Agent capacity</h2><p><span class="metric">1 / ${model.agents.max}</span><br><span class="muted">agent slots active</span></p>${bar(50)}<p><strong>D-04</strong> · Prototype and evidence</p><a href="#/agents">Open agent operations →</a></section><section class="card outcomes"><p class="eyebrow">Current phase</p><h2>${model.phase.id} · ${model.phase.title}</h2>${bar(model.phase.progress)}${model.outcomes.map(o=>`<div class="outcome-row"><strong>${e(o.title)}</strong><div>${bar(o.progress)}</div><span>${o.progress}%</span></div>`).join('')}<p class="muted">Progress rolls up completed scoped work. It does not prove the product outcome.</p></section></div>`}
function plan(){const q=query(),view=q.get('view')||'outline',selected=q.get('selected')||'PW-01';const href=v=>`#/plan?view=${v}&selected=${selected}`;main.innerHTML=`<div class="header"><div><p class="eyebrow">Development plan</p><h1>${model.phase.id} · ${model.phase.title}</h1><p class="lede">Outcomes, contributing work, progress and the dependencies that shape the route.</p></div><a class="button" href="#/product">Product direction ↗</a></div><div class="switch" aria-label="Plan view"><a class="button ${view==='outline'?'active':''}" href="${href('outline')}">Outcome outline</a><a class="button ${view==='map'?'active':''}" href="${href('map')}">Dependencies & rollout</a><button disabled title="Evaluate after outline and dependency views prove useful">Board · later</button></div>${view==='outline'?outline(selected):map(selected)}`}
function outline(selected){return `<div class="plan-layout"><section class="tree" aria-label="Plan hierarchy">${model.outcomes.map(o=>`<details class="node" open><summary><strong>${e(o.title)}</strong><span>${o.progress}% · ${o.health}</span></summary><div class="node-body">${model.clusters.filter(c=>c.outcome===o.id).map(c=>`<details class="node" open><summary><strong>${e(c.title)}</strong><span>${c.progress}%</span></summary><div class="node-body">${model.items.filter(i=>i.cluster===c.id).map(i=>`<a class="work-item ${i.id===selected?'active':''}" href="#/plan?view=outline&selected=${i.id}"><span><strong>${i.id} · ${e(i.title)}</strong><small>${i.depends.length?`Depends on ${i.depends.join(', ')}`:'No blocking dependency'} · ${e(i.action)}</small></span>${state(i.state)}</a>`).join('')}</div></details>`).join('')}</div></details>`).join('')}</section>${detail(selected)}</div>`}
function detail(id){const i=item(id);if(!i)return `<aside class="detail"><h2>Work item unavailable</h2><p>No other item is substituted.</p></aside>`;return `<aside class="detail"><p class="eyebrow">${i.id}</p><h2>${e(i.title)}</h2>${state(i.state)}${fixture()==='stale'?'<p class="error"><strong>Foundational input changed.</strong><br>Product outcome r3 is now r4; reassess before authorization or acceptance.</p>':''}${bar(i.progress)}<div class="fact"><span>Supports</span><strong>${e(model.outcomes.find(o=>o.id===cluster(i.cluster).outcome).title)}</strong></div><div class="fact"><span>Primary planning home</span><strong>${e(cluster(i.cluster).title)}</strong></div><div class="fact"><span>Consumes</span><strong>${e(i.source)}</strong></div><div class="fact"><span>Dependencies</span><strong>${i.depends.join(', ')||'None'}</strong></div>${i.action!=='None'?`<div class="inline-action"><strong>${e(i.action)}</strong><p>Handle this action in its authoritative record, then return to the same plan node.</p><button>Open fixture action</button></div>`:''}<a href="#/item/${i.id}">Open full work item →</a></aside>`}
function map(selected){const phases=['Groundwork','Shape','Build','Review'];return `<section class="map" tabindex="0" aria-label="Dependency and rollout map"><div class="map-grid"><div class="map-head">Feature cluster</div>${phases.map(p=>`<div class="map-head">${p}</div>`).join('')}${model.clusters.map(c=>`<div><strong>${e(c.title)}</strong><small>${c.progress}%</small></div>${phases.map(p=>`<div class="map-cell">${model.items.filter(i=>i.cluster===c.id&&i.phase===p).map(i=>`<a class="${i.state==='Blocked'?'blocked':''} ${i.id===selected?'active':''}" href="#/plan?view=map&selected=${i.id}">${i.id}<small>${e(i.state)}</small></a>`).join('')}</div>`).join('')}`).join('')}</div></section><p class="dependency-note"><strong>Critical chain:</strong> D-04 → PW-01 → PW-02 → PW-05. B-03B independently blocks PW-04. Select a node to preserve it when switching views.</p>${detail(selected)}`}
function agents(){if(fixture()==='zero'){main.innerHTML=`<p class="eyebrow">Agent operations</p><h1>Execution capacity and queue</h1><section class="empty-state"><h2>No active or eligible work</h2><p>2 configured slots are available. Planned work still requires dependencies, current inputs, and authorization.</p><a class="button" href="#/plan">Open development plan</a></section>`;return}const a=model.agents.active[0];main.innerHTML=`<div class="header"><div><p class="eyebrow">Agent operations</p><h1>Execution capacity and queue</h1><p class="lede">Attempts and resources here; purpose, dependencies and product history stay on the work item.</p></div><span class="pill run">1 of ${model.agents.max} active</span></div><section class="slots"><article class="slot"><p class="eyebrow">Slot 1 · active</p><h2>${a.id}</h2><p><a href="#/plan?selected=${a.item}">${a.item}</a> · ${a.stage}</p>${bar(72)}<p>${a.progress} · ${a.elapsed}</p><div class="activity"><time>Now</time><p>Building connected prototype and checking cross-view identity.</p></div><div class="activity"><time>7m ago</time><p>Pattern comparison completed.</p></div></article><article class="slot empty"><p class="eyebrow">Slot 2 · available</p><h2>No eligible item</h2><p>PW-01 awaits owner selection. B-03B still has review/recovery inputs.</p></article></section><div class="queue-grid"><section class="panel"><h2>Ready queue</h2>${model.agents.queue.map(q=>`<a class="queue-row" href="#/plan?selected=${q.item}"><span><strong>${q.rank}. ${q.item} · ${e(item(q.item).title)}</strong><small>${e(q.reason)}</small></span>${state(item(q.item).state)}<span>›</span></a>`).join('')}<h2>Interventions and handoffs</h2>${model.agents.interventions.map(x=>`<a class="queue-row" href="#/plan?selected=${x.item}"><span><strong>${x.attempt} · ${x.item}</strong><small>${e(x.note)}</small></span><span class="pill ${x.status==='Failed'?'decision':x.status==='Ready for review'?'review':''}">${e(x.status)}</span><span>›</span></a>`).join('')}</section><aside class="panel"><h2>Run history</h2>${model.agents.history.map(h=>`<div class="fact"><span>${h.attempt} · ${h.duration}</span><strong><a href="#/plan?selected=${h.item}">${h.item}</a></strong><p>${e(h.result)}</p></div>`).join('')}<p class="muted">Attempt history links to the work item; it does not replace product-work history.</p></aside></div>`}
function intake(){const grouped=sessionStorage.getItem('v5-grouped')==='yes';const signals=fixture()==='many'?Array.from({length:24},(_,n)=>({...model.signals[n%model.signals.length],id:`SIG-${n+1}`})):model.signals;main.innerHTML=`<div class="header"><div><p class="eyebrow">Intake</p><h1>Turn signals into intentional work.</h1><p class="lede">Preserve raw input, then group, connect, clarify, defer, or propose a plan change.</p></div><span class="pill decision">Triage due · ${signals.length} signals</span></div><div class="triage"><section class="panel"><h2>Unsorted and resurfaced</h2>${signals.map((s,n)=>`<label class="signal"><input type="checkbox" value="${s.id}" ${[1,2,4].includes(n)?'checked':''}><span><strong>${s.id} · ${e(s.text)}</strong><small>${s.group} · ${s.age}</small></span><span class="pill">${s.kind}</span></label>`).join('')}</section><aside class="panel"><h2>Triage session</h2><p>3 selected signals appear to describe one Work-planning need.</p><button class="primary" id="group">Group as “Work planning model”</button><p id="group-status" role="status">${grouped?'Grouped. Originals retained with backlinks.':''}</p>${grouped?`<div class="notice"><strong>Proposed plan change</strong><p>Revise D-04 feature model from lifecycle lanes to Overview, Plan, Agent Operations and Intake.</p><p>State: proposal only · no task or authorization created.</p><a href="#/plan?selected=D-04">Open related plan item →</a></div>`:''}<div class="fact"><span>Other actions</span><strong>Link existing · request clarification · defer · archive</strong></div></aside></div>`;document.querySelector('#group').onclick=()=>{sessionStorage.setItem('v5-grouped','yes');intake()}}
function itemPage(id){const i=item(id);main.innerHTML=i?`<nav class="crumb"><a href="#/plan?selected=${i.id}">Development plan</a><span>›</span><span>${i.id}</span></nav><p class="eyebrow">Work item</p><h1>${e(i.title)}</h1><p class="lede">${e(i.source)}</p>${detail(i.id)}<section class="card"><h2>History</h2><p>Planning revisions, execution attempts, questions, artifacts and reviews stay linked here without collapsing into one state machine.</p></section>`:`<section class="empty-state"><h1>Work item not found</h1><p>The requested identity remains visible; no replacement is shown.</p><a class="button" href="#/plan">Back to plan</a></section>`}
function placeholder(){main.innerHTML=`<section class="empty-state"><h1>Outside this experiment</h1><p>D-04 represents Work and its links to Product/Design revisions.</p><a class="button" href="#/work">Open Work overview</a></section>`}
function render(){setNav();const path=(location.hash.split('?')[0].slice(1)||'/work').split('/').filter(Boolean);if(path[0]==='work')overview();else if(path[0]==='plan')plan();else if(path[0]==='agents')agents();else if(path[0]==='intake')intake();else if(path[0]==='item')itemPage(path[1]);else placeholder();main.focus({preventScroll:true})}addEventListener('hashchange',render);render();
