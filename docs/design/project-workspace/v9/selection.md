---
id: project-workspace-v9-selection
kind: owner-selection
status: accepted-for-implementation
updated: 2026-09-21
packet: D-04R
---

# Work composition selection

The owner reviewed the v9 reference-led composition and instructed: “alright, go ahead and build.” This accepts v9 as the implementation direction and selects its recommended milestone-scoped table as the default Development Plan projection. The timeline remains a later contextual projection when authoritative dates and overlap exist.

The chat instruction authorizes the bounded local portal implementation, repository edits, schema/API changes needed for the accepted Work surfaces, automated checks, and local preview under DEC-029. It does not authorize spending, external services or writes, public deployment, release, unattended execution, or owner acceptance of the resulting implementation.

Implementation must preserve truthful data semantics. Existing work items, requests, decisions, dependencies, and activity history may be connected. Milestone progress, agent capacity, server health, queue eligibility, and spend enforcement must use authoritative records or render as unavailable; prototype numbers cannot become live claims. The stale/open decision-input dead end must gain an understandable recovery path that refreshes a ready work item for renewed review without authorizing it.
