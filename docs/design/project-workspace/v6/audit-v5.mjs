import { chromium } from '/tmp/app-builder-d01b-browser/node_modules/playwright/index.mjs';
import {writeFileSync} from 'node:fs';
const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});const out='/mnt/c/Users/henry/VSCode Projects/app-builder/docs/design/project-workspace/v6/';const base='http://127.0.0.1:4310/reviews/project-workspace-v5/index.html';const results={};
for(const route of ['work','plan','agents','intake']){await page.goto(base+'#/'+route);await page.locator('main h1').waitFor();await page.screenshot({path:out+'before-'+route+'.png',fullPage:true});results[route]={title:await page.locator('h1').innerText()};}
for(const checkbox of await page.getByRole('checkbox').all()) await checkbox.uncheck();results.intake.checked=await page.locator('input:checked').count();results.intake.summary=await page.locator('.triage aside').innerText();await page.getByRole('button',{name:/Group as/}).click();results.intake.groupedWithZeroSelection=await page.getByText('Grouped. Originals retained with backlinks.').isVisible();
await page.goto(base+'#/plan');const before=await page.locator('main').innerHTML();await page.getByRole('button',{name:'Open fixture action'}).click();results.plan.actionChangesContent=(await page.locator('main').innerHTML())!==before;
await page.setViewportSize({width:390,height:844});results.plan.detailTitleVisible=await page.locator('.detail h2').isVisible();results.plan.detailTitleDisplay=await page.locator('.detail h2').evaluate(e=>getComputedStyle(e).display);await page.screenshot({path:out+'before-plan-narrow.png',fullPage:true});
writeFileSync(out+'audit-results.json',JSON.stringify(results,null,2)+'\n');console.log(JSON.stringify(results));await browser.close();
