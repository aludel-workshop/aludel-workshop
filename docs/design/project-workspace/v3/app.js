const items=[
 {id:'B-02',title:'Product records and dependencies',lane:'history',state:'Recorded complete',kind:'Historical packet',summary:'Versioned proposals and decisions with dependency-specific staleness.',source:'Execution plan + status + evidence',evidence:'Domain, browser, restart and accessibility checks',caution:'Imported completion assertion. No historical request, authorization or attempt is invented.'},
 {id:'B-03',title:'Real run and preview review',lane:'plan',state:'Blocked',kind:'Planned packet',summary:'Connect authorized work to a real agent-built, immutable candidate review.',source:'Execution plan revision',evidence:'R-03, R-05 and R-06 evidence; B-03A supervised bridge',caution:'Still requires source identity, worker recovery and review evidence. Planning does not authorize execution.'},
 {id:'D-02',title:'Project workspace strategy',lane:'history',state:'Submitted',kind:'Real portal work',summary:'Mapped product records, feature groups, contextual rationale and project creation.',source:'Portal work WORK-17d6…',evidence:'D-02 strategy manifest and owner decision r2',caution:'Submitted evidence is not artifact acceptance.'},
 {id:'D-03',title:'Work planning/history design',lane:'active',state:'Running',kind:'Real portal work',summary:'Design this first slice and prove safe reconciliation on disposable data.',source:'Portal work WORK-7a16…',evidence:'Current design experiment',caution:'This page is the experiment, not the implemented feature.'},
 {id:'REV-D01E',title:'Task and review composition',lane:'reviews',state:'Direction selected',kind:'Design review',summary:'Owner-selected task/review composition baseline.',source:'D-01E review record',evidence:'Scoped owner feedback and browser evidence',caution:'Selection covers the recorded composition, not production artifact acceptance.'}
];
const main=document.querySelector('main');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const laneLabel={plan:'Plan',history:'History',active:'Active',reviews:'Reviews'};
function navState(){document.querySelectorAll('a').forEach(a=>a.classList.toggle('active',a.hash===location.hash));}
function collection(lane){
 const fixture=new URLSearchParams(location.search).get('fixture');
 if(fixture==='error'){main.innerHTML=`<p class="overline">Work</p><h1>${laneLabel[lane]}</h1><section class="empty"><h2>Work could not be loaded</h2><p>The collection state is unknown. No zero count is shown.</p><a class="button primary" href="${location.pathname}#/work/${lane}">Try again</a></section>`;return}
 let list=items.filter(i=>i.lane===lane);if(fixture==='zero')list=[];if(fixture==='many')list=Array.from({length:24},(_,n)=>({...items[1],id:`PLAN-${String(n+1).padStart(2,'0')}`,title:`Planning fixture ${n+1}`})); const title=laneLabel[lane];
 main.innerHTML=`<p class="overline">Work</p><h1>${title}</h1><p class="lede">${lane==='plan'?'Prioritized work toward product outcomes. Planning and authorization remain separate.':lane==='history'?'Completed, cancelled, and superseded work with original evidence and provenance.':lane==='reviews'?'Exact artifacts awaiting or retaining scoped judgment. Release remains separate.':'Authorized, running, blocked, or waiting work.'}</p>
 <div class="tabs">${['plan','active','reviews','history'].map(x=>`<a class="${x===lane?'active':''}" href="#/work/${x}">${laneLabel[x]}</a>`).join('')}</div>
 ${lane==='plan'?'<div class="actions"><a class="button primary" href="#/work/plan/new">New planning item</a><a class="button" href="#/work/reconcile">Review imported work</a></div>':''}
 ${fixture==='many'?'<p class="notice">Showing 24 of 24 planning items · ordered by priority, then stable ID.</p>':''}${list.length?`<div class="list">${list.map(i=>`<a class="row" href="#/work/${lane}/${i.id}"><span><strong>${i.id} · ${esc(i.title)}</strong><small>${esc(i.kind)} · ${esc(i.summary)}</small></span><span class="badge ${i.state.includes('complete')?'complete':i.state==='Blocked'?'blocked':lane==='reviews'?'review':''}">${esc(i.state)}</span><span aria-hidden="true">›</span></a>`).join('')}</div>`:'<section class="empty"><h2>Nothing here yet</h2><p>This state says what belongs here and keeps the creation action scoped.</p></section>'}`;
}
function detail(lane,id){const i=items.find(x=>x.id===id&&x.lane===lane);if(!i){main.innerHTML=`<a href="#/work/${lane}">← Back to ${laneLabel[lane]||'Work'}</a><section class="empty"><h1>Work record not found</h1><p>The link may be stale or belong to another project. No other record is substituted.</p></section>`;return}
 main.innerHTML=`<nav class="crumb" aria-label="Breadcrumb"><a href="#/work/${lane}">Work</a><span>›</span><span>${laneLabel[lane]}</span><span>›</span><span>${i.id}</span></nav><p class="overline">${esc(i.kind)}</p><h1>${esc(i.title)}</h1><p class="lede">${esc(i.summary)}</p><div class="facts"><div class="fact"><span>Lifecycle</span><strong>${esc(i.state)}</strong></div><div class="fact"><span>Identity</span><strong>${i.id}</strong></div><div class="fact"><span>Authority</span><strong>${lane==='history'?'Source-backed assertion':lane==='reviews'?'Scoped owner judgment':'Plan / portal workflow'}</strong></div></div><p class="notice">${esc(i.caution)}</p><section class="section"><h2>Evidence and provenance</h2><div class="card"><p><strong>Source</strong><br>${esc(i.source)}</p><p><strong>Evidence</strong><br>${esc(i.evidence)}</p></div></section><section class="section"><h2>Related lifecycle</h2><div class="timeline"><article><strong>Intent and plan</strong><p>Stable product purpose and proposed sequence.</p></article><article><strong>Executable work</strong><p>${lane==='history'?'No reconstructed execution event.':lane==='active'?'Current authorization and attempt remain in the portal workflow.':'Requires a separately prepared and authorized task.'}</p></article><article><strong>Review and release</strong><p>Review binds to an exact artifact. Release is always a later authority.</p></article></div></section>`;
}
function form(){const saved=sessionStorage.getItem('d03-plan-draft')||'';main.innerHTML=`<a href="#/work/plan">← Back to Plan</a><p class="overline">Work · Plan</p><h1>New planning item</h1><p class="lede">Capture intended work without making it executable.</p><form class="form" id="plan-form"><label>Title<input name="title" value="${esc(saved)}"></label><label>Outcome<textarea name="outcome" rows="4">Make existing product work visible and trustworthy.</textarea></label><label>Horizon<select name="horizon"><option>Now</option><option>Next</option><option>Later</option></select></label><p class="notice">Saving a plan revision does not create a request, task, authorization, attempt, review, or release.</p><div class="actions"><button class="primary" type="submit">Save fixture draft</button><a class="button" href="#/work/plan">Cancel</a></div><p id="form-status" role="status"></p></form>`;const f=document.querySelector('#plan-form');f.title.addEventListener('input',e=>sessionStorage.setItem('d03-plan-draft',e.target.value));f.addEventListener('submit',e=>{e.preventDefault();document.querySelector('#form-status').textContent='Fixture draft saved. No work was authorized.'})}
function reconcile(){main.innerHTML=`<a href="#/work/plan">← Back to Plan</a><p class="overline">Work · Reconciliation review</p><h1>Review imported work</h1><p class="lede">Turn selected source assertions into useful planning/history records without rewriting history.</p><p class="warning"><strong>1 conflict needs judgment.</strong> Status says B-03A is complete within its supervised boundary; the execution plan says full B-03 remains incomplete. Proposed mapping preserves both scopes.</p><div class="mapping" tabindex="0" aria-label="Proposed source mappings"><table><thead><tr><th>Source assertion</th><th>Proposed object</th><th>Provenance</th><th>Action</th></tr></thead><tbody><tr><td>B-02 complete</td><td>B-02 · Historical packet · recorded complete</td><td>Execution plan + status + evidence hashes</td><td>Ready to include</td></tr><tr><td>B-03A complete locally</td><td>B-03A · Historical packet · bounded completion</td><td>Status + supervised-cycle evidence</td><td>Ready to include</td></tr><tr class="selected-row"><td>B-03 planned / incomplete</td><td>B-03 · Planned packet · blocked</td><td>Execution plan + current status</td><td>Preserve distinct scope</td></tr><tr><td>D-02 submitted work</td><td>D-02 · Linked real work</td><td>Existing portal work/event identity</td><td>Link; do not recreate</td></tr></tbody></table></div><div class="compare"><section class="card"><h2>Will be created</h2><p>Plan/history identities, revisions, source assertions, dependencies, and links to existing work.</p></section><section class="card"><h2>Will not be created</h2><p>Historical requests, authorizations, attempts, owner acceptance, releases, or credentials.</p></section></div><div class="actions"><button class="primary" id="apply-fixture">Apply to disposable fixture</button><a class="button" href="#/work/reconcile/error">Preview changed-source state</a></div><p id="apply-status" role="status"></p>`;document.querySelector('#apply-fixture').onclick=()=>document.querySelector('#apply-status').textContent='Fixture result: 4 mapped, 0 duplicate, 0 live portal writes. See reconciliation evidence for transaction checks.'}
function reconcileError(){main.innerHTML=`<a href="#/work/reconcile">← Back to reconciliation</a><section class="empty"><h1>Review is out of date</h1><p>B-03 source content changed after this preview. No mappings were applied. Refresh the proposed mappings and review the new differences.</p><a class="button primary" href="#/work/reconcile">Refresh reconciliation</a></section>`}
function render(){navState();const p=(location.hash.slice(1)||'/work/plan').split('/').filter(Boolean);if(p[0]!=='work'){main.innerHTML='<section class="empty"><h1>Outside this experiment</h1><p>This bounded prototype represents Work only.</p><a class="button" href="#/work/plan">Open Work</a></section>'}else if(p[1]==='reconcile'&&p[2]==='error')reconcileError();else if(p[1]==='reconcile')reconcile();else if(p[1]==='plan'&&p[2]==='new')form();else if(p[2])detail(p[1],p[2]);else collection(p[1]||'plan');main.focus({preventScroll:true})}
addEventListener('hashchange',render);render();
